# :demos:agent

Android 端对话式 Agent Demo,展示 SDK 核心能力在 Compose UI 下的集成方式。
涵盖多轮对话、工具调用、Skill 加载、MCP 集成、Subagent 编排。

## 配置

复制 `local.properties.example` 为 `local.properties` 并按要求完成配置。真实密钥文件已被根 `.gitignore` 忽略。
