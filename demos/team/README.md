# :demos:team

Android 端实时语音(S2S)Agent Demo,在 `:demos:agent` 文本对话基础上叠加
`:realtime` 模块的语音输入输出与 `:team` 模块的多 Agent 编排能力。

## 配置

复制 `local.properties.example` 为 `local.properties` 并按要求完成配置。真实密钥文件已被根 `.gitignore` 忽略。
