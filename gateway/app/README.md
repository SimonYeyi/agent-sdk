# :gateway:app

Android Gateway 客户端（包名 `io.github.yeyi.agent.gateway.app`，Compose UI）。

## 配置

复制 `local.properties.example` 为 `local.properties` 并填入 `ANTHROPIC_*` / `FEISHU_APP_*` 字段。真实密钥文件已被根 `.gitignore` 忽略。
